# MDXProvider CLI 示例

这是一个使用 **MDXProvider** 的示例。此内容通过命令行脚本处理。

## 标题会是绿色的

在 MDXProvider 中定义的组件可以直接在 MDX 中使用，无需导入。

<Highlight>
  这段文字使用了 Highlight 组件，它是通过 MDXProvider 全局提供的。
</Highlight>

<Wrapper>
  ```js
  // 代码示例
  const example = "这是一个示例";
  console.log(example);
  ```
</Wrapper> 